namespace Nekinu_Editor.UserCustomEditor
{
    public class LoadedUserEditors
    {
        //Find all custom editors at runtime, or when project is hot reloaded.
        //Make the list accessible, so the properties panel can look for the editors, once a class comes by that does have a custom editor
    }
}